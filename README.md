# Customer API 

Demonstrates a REST API using the ASP.NET Web API framework. 

The customer resource has GET, POST, PUT, PATCH and DELETE endpoints. Request validation on provided using fluent validation framework.

Postgres database is used for persistence with dapper and npgsql. Query and Commands provide data access layers.

Open API documentation using swagger is included.

The unit test project is included to test each endpoint. 


